# get_next_line
A C implementation of the getline function, reading files line by line using static buffers and file descriptors. Includes bonus support for multiple file descriptors as part of the 42 School curriculum.
